package ${package}.dao;

import ${package}.domain.DO.${entity}DO;
import ${package}.domain.query.${entity}Query;
import ${package}.mapper.${entity}Mapper;
import org.springframework.stereotype.Repository;
import com.baomidou.mybatisplus.extension.service.impl.ServiceImpl;
import java.util.List;
import javax.annotation.Resource;

/**
 * ${comments}
 * 
 * @author ${author}
 * @date ${datetime}
 * @version 1.0
 */
@Repository
public class ${className}DaoImpl extends ServiceImpl<${className}Mapper, ${className}DO> implements ${className}Dao {

    @Resource
    ${className}Mapper mapper;

    @Override
    public List<${className}DO> findAll(${className}Query query) {
        return mapper.findAll(query);
    }

}

