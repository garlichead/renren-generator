package ${package}.dao;

import ${package}.domain.DO.${className}DO;
import ${package}.domain.query.${className}Query;
import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import org.apache.ibatis.annotations.Mapper;
import java.util.List;
/**
 * ${comments}
 * 
 * @author ${author}
 * @date ${datetime}
 * @version 1.0
 */
@Mapper
public interface ${className}Mapper extends BaseMapper<${className}DO> {

    /**
     * 按条件查询
     *
     * @param query
     * @return
     */
    List<${className}DO> findAll(${className}Query query);

}
